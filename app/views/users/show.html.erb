<section class="user-section">
  <h2 class="d-inline"><%= "Name: #{@user.name}" %></h2>
  <%= new_friendship_request(@user) %>
  <article class="timeline">
    <h3>Recent posts:</h3>
    <ul class="posts">
      <%= render @posts %>
    </ul>
  </article>
</section>
